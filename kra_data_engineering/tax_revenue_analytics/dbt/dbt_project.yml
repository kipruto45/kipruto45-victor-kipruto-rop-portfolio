name: 'kra_revenue'
version: '1.0.0'
config-version: 2

profile: 'kra_revenue'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  kra_revenue:
    staging:
      +materialized: view
    marts:
      +materialized: table
