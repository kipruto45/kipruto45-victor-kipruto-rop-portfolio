saf_profile:
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DB_HOST', 'localhost') }}"
      user: saf_admin
      password: saf_password
      port: 5432
      dbname: safaricom_warehouse
      schema: public
      threads: 4
  target: dev
