name: 'equity_analytics'
version: '1.0.0'
config-version: 2
profile: 'equity_profile'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  equity_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
