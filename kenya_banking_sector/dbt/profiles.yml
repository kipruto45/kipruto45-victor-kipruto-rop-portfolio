sector_profile:
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DB_HOST', 'localhost') }}"
      user: sector_admin
      password: sector_password
      port: 5432
      dbname: sector_warehouse
      schema: public
      threads: 4
  target: dev
